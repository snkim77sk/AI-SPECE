# 버전 관리 기준

이 저장소는 2026-08-15 롤백 안정화 기준부터 단순 버전 체계를 사용합니다.

- **1.0**: 롤백 후 안정 기준판
- **1.1, 1.2, 1.3 ...**: 기존 구조를 유지하는 검증된 기능 보완/오류 수정
- **2.0**: 데이터 수집 엔진 또는 프로그램 구조를 크게 변경하는 차기 주요판
- 이후도 같은 방식으로 **2.1, 2.2 ... / 3.0** 순으로 관리

운영 원칙:

1. `main`은 실제 배포 후보 코드만 유지합니다.
2. 주요 안정판은 `release/<버전>` 브랜치로 고정 보관합니다.
3. 대규모 수정 전 현재 `main`은 `archive/...` 브랜치에 백업합니다.
4. 실데이터 수집 검증이 끝나기 전에는 다음 안정 버전으로 올리지 않습니다.
5. `VERSION.txt`, `/health` 버전, 앱 표시 버전은 동일하게 맞춥니다.
6. `release/1.0`은 Cafe24 기동 및 실데이터 검증을 통과한 현재 1.0 커밋으로 고정합니다.
