# 버전 관리 기준

## 안정 버전 체계

- **1.0**: 기존 안정판/복구 기준판 (`release/1.0` 보관)
- **2.0**: v2.6.3 화면 구조를 유지하면서 API 수집엔진과 실데이터를 완전 초기화한 새 기준판
- **2.1**: 검증된 2.0 단일 수집엔진으로 쇼핑몰·물품입찰·용역을 2시간마다 자동 갱신
- **2.2**: 2.1 자동수집의 중복실행·재시작·API 한도·상태감시 안정화
- **2.3**: 2.2 안정 구조를 유지하면서 2025-01-01부터 G2B 과거자료를 별도 체크포인트 방식으로 구축
- **2.4 ...**: 2.3 안정 구조를 유지하는 검증된 후속 기능
- **3.0**: 데이터 구조 또는 프로그램 아키텍처를 다시 크게 변경할 때만 사용

## 2.0 원칙

1. 쇼핑몰·입찰·용역 수집은 `collector_v200.py` 단일 엔진만 사용한다.
2. `sinsung_v253_patch.py` ~ `sinsung_v263_patch.py`는 과거 이력 보존용이며 실행하지 않는다.
3. 2.0 최초 실행 시 쇼핑몰, 입찰/용역, 예산, 수집로그를 1회 초기화한다.
4. 사용자 계정, 회사 설정, 나라장터 API 키, 지방재정365 API 키, 세션 보안값은 유지한다.
5. `VERSION.txt`, `/health`, 앱 표시 버전은 항상 같은 버전을 사용한다.
6. 안정판은 `release/<버전>` 브랜치로 고정 보관하고, 대규모 변경 전에는 `archive/...` 브랜치를 만든다.

## 2.1 자동수집 원칙

1. 수동수집과 자동수집은 동일한 `collector_v200.py` 수집엔진을 사용한다.
2. 기본 자동수집 주기는 2시간이다.
3. 매 실행마다 최근 14일을 재조회하고 `UPSERT`로 신규·변경 자료를 갱신한다.
4. 쇼핑몰 → 물품입찰 → 용역 순서로 실행하며 각 수집 결과를 별도로 기록한다.
5. 한 항목이 실패해도 나머지 항목은 계속 수집한다.
6. 실패 시 1분마다 재시도하지 않고 다음 정상 2시간 주기에서 다시 시도한다.
7. 지방재정365 예산 자동수집은 나라장터 2시간 스케줄과 별도 설정으로 유지한다.

## 2.2 자동수집 안정화 원칙

1. 자동수집과 수동수집은 동시에 실행하지 않는다. 자동수집 중 수동수집 요청은 대기 안내하고, 수동수집 중에는 자동수집이 시작되지 않는다.
2. Cafe24 재시작 시 이전 프로세스의 `수집중` 상태와 수동수집 플래그를 자동 복구한다.
3. 자동수집 주기는 2시간으로 고정하고 정상 운영 시 최근 14일을 중복조회해 `UPSERT`한다.
4. 장시간 서버 중단 후에는 마지막 성공시각을 기준으로 최대 30일까지 조회범위를 자동 확대해 누락 가능성을 줄인다.
5. 쇼핑몰 API와 입찰 API 각각 일일 한도의 마지막 100회는 안전여유로 남겨 자동수집이 한도를 끝까지 소진하지 않게 한다.
6. scheduler heartbeat, 현재 수집원, 다음 예정시각, 실행시간, 연속오류 횟수, 마지막 성공시각을 영구 설정에 기록한다.
7. API 오류가 발생해도 가능한 나머지 수집원은 계속 진행하고 결과를 `완료`, `부분오류`, `한도대기`로 구분한다.
8. 기존 실데이터, 사용자, API 키, 회사설정은 2.2 전환 시 삭제하거나 초기화하지 않는다.

## 2.3 과거자료 구축 원칙

1. 구축 범위는 **2025-01-01부터 시작 시점의 현재 날짜까지**이며 대상은 쇼핑몰·물품입찰·용역 G2B 실데이터다.
2. 지방재정365 예산은 기존 일일 수집을 유지하며 2.3 G2B 과거구축과 분리한다.
3. 7일 단위 체크포인트를 사용하고 각 구간에서 쇼핑몰 → 물품입찰 → 용역 순으로 진행한다.
4. 과거구축은 `collector_v200.py`의 동일한 API 요청·파서·UPSERT 원리를 재사용하지만 최근 자동수집 상태값과 과거 진행상태를 분리해 기록한다.
5. 과거구축은 2시간 자동수집 예정시간이 가까워지면 자동으로 양보하고 자동수집 완료 후 이어서 진행한다.
6. API 종류별 일일 한도에서 마지막 **200회**를 실시간 수집용으로 남기며, 여유구간에 도달하면 다음 날 자동 재개한다.
7. API 한도, Cafe24 재시작, 수동수집 대기 시 현재 날짜·수집원 체크포인트를 보존하고 이어서 진행한다.
8. 사용자가 중지를 요청할 수 있으며 재개 시 완료된 구간은 다시 처음부터 시작하지 않는다.
9. 과거 구축 중 오류가 발생하면 해당 구간·수집원에서 멈추고 상태와 오류를 표시하며, 사용자가 같은 지점부터 재개할 수 있다.
10. 2.3 전환 시 기존 실데이터, 사용자, 회사설정, 나라장터/예산 API 키를 삭제하거나 초기화하지 않는다.
